# Parent Task ID: ROO#TASK_20250525141010_A1B2C3D4
# Parent Task Context: [context.md](/.rooroo/tasks/ROO#TASK_20250525141010_A1B2C3D4/context.md)
# This Sub-task: 1 of 5

## Goal for Expert (rooroo-developer):
Set up a basic FastAPI application structure.
- Initialize a new Python project (e.g., using `uv init` or manually creating `pyproject.toml`).
- Add FastAPI and Uvicorn as dependencies.
- Create a `main.py` file with a minimal FastAPI app instance.
- Implement a simple root endpoint (e.g., `/`) that returns a JSON response like `{"message": "Hello World"}`.
- Ensure the application can be run using Uvicorn (e.g., `uvicorn main:app --reload`).
- Create a basic `.gitignore` file.